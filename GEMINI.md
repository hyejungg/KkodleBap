이 문서를 참고하여 꼬들밥(KkodleBap)을 완성합니다. 

# 🍚 꼬들밥 (KkodleBap) - 웹 버전
> 한글 자모 퍼즐 게임
> “하루 한 그릇, 자모를 모아 만드는 꼬들한 퍼즐!”

## 1. 목적 및 개요
- 사용자는 한글 자모 6개를 조합하여 정답을 맞히는 퍼즐 게임을 진행하며, 귀여운 일러스트와 피드백 애니메이션을 통해 몰입감을 제공한다.
  - 일러스트 및 디자인 정보는 피그마 문서를 참고한다.
- 반응형 디자인, 브라우저 호환성, 다크모드를 고려한다.

##  2. 핵심 기능 명세서
### 기능별 게임 로직
| 기능             | 설명                                              |
| -------------- | ----------------------------------------------- |
| **자모 조합 추측**   | 초성, 중성, 종성을 조합해 낱말을 완성. 총 6글자 입력 가능             |
| **정답 판별 로직**   | 입력 단어가 정답과 일치하는지 비교. 자모별 정답 여부 표시 (색상 또는 애니메이션) |
| **기회 제한**      | 하루 1문제, 최대 6회 시도 가능                             |
| **점수 공유**      | 정답/실패 결과 요약 복사 & 공유 기능 제공                       |
| **스토리/튜토리얼**   | 게임 첫 시작 시 귀여운 일러스트와 함께 튜토리얼 화면 제공               |
| **실패 시 피드백**   | 오답 시 "텅 빈 밥그릇" 일러스트 등장                          |

### UI 구성 요소
| 컴포넌트           | 설명                            |
| -------------- | ----------------------------- |
| `SplashView`   | 로고 및 소개 애니메이션 화면              |
| `GameView`     | 메인 게임 화면 (자모 조합, 입력창, 키보드 포함) |
| `KeyboardView` | 가상 키보드 UI (초/중/종성 별 구분)       |
| `ResultView`   | 정답 성공/실패 후 결과 요약              |
| `TutorialView` | 일러스트 기반 사용법 안내 화면             |


##  3. 기술 스택 및 구조 설계
###  주요 기술 스택
- 프레임워크: Vue 3 (Composition API 기반)
- 상태관리: Pinia 또는 Composition API 자체 상태
- 라우팅: Vue Router
- 스타일링: Tailwind CSS + 사용자 정의 일러스트
- 빌드/배포: Vite, Netlify/Vercel
- 애니메이션: Vue Transition 활용
- 한글 자모 분해/조합: 사용자 정의 유틸 모듈 (e.g., jamo.ts)

### 4. 디렉토리 구조
```
kkodlebap-web/
├── public/               # 정적 리소스 (일러스트, 폰트 등)
├── src/
│   ├── assets/           # 이미지, 스타일 등
│   ├── components/       # 게임 관련 UI 컴포넌트
│   ├── views/            # 라우팅 단위 뷰
│   ├── composables/      # 상태 관리 (useGameState 등)
│   ├── utils/            # 자모 유틸, 정답 체크 로직 등
│   ├── router/           # Vue Router 설정
│   └── App.vue           # 루트 컴포넌트
├── index.html
└── vite.config.ts
```

### 5. 게임 핵심 로직 설계
1. 구성 요소 정의
```ts
const CHOSEONG = 'ㄱㄲㄴㄷㄸㄹㅁㅂㅃㅅㅆㅇㅈㅉㅊㅋㅌㅍㅎ';
const JUNGSEONG = 'ㅏㅐㅑㅒㅓㅔㅕㅖㅗㅘㅙㅚㅛㅜㅝㅞㅟㅠㅡㅢㅣ';
const JONGSEONG = ['', 'ㄱ', 'ㄲ', 'ㄳ', 'ㄴ', 'ㄵ', 'ㄶ', 'ㄷ', 'ㄹ',
  'ㄺ', 'ㄻ', 'ㄼ', 'ㄽ', 'ㄾ', 'ㄿ', 'ㅀ', 'ㅁ', 'ㅂ',
  'ㅄ', 'ㅅ', 'ㅆ', 'ㅇ', 'ㅈ', 'ㅊ', 'ㅋ', 'ㅌ', 'ㅍ', 'ㅎ'
];

const complexInitialMap = { 'ㄲ': ['ㄱ','ㄱ'], 'ㄸ': ['ㄷ','ㄷ'], ... };
const complexMedialMap = { 'ㅘ': ['ㅗ','ㅏ'], 'ㅙ': ['ㅗ','ㅐ'], ... };
const complexFinalMap = { 'ㄳ': ['ㄱ','ㅅ'], 'ㄵ': ['ㄴ','ㅈ'], ... };
```
2. 단어 풀 로딩
- 목적
  - 정답 단어(answerWords) 및 전체 단어 목록(allWords)을 로딩하고 캐싱
  - 6자모 조건에 맞는 정답 단어 필터링
- 게임 시작 시 단어 1개를 생성하여 게임이 끝나기 전까지 클라이언트 메모리에 유지

3. 단어 유효성 검사
- 사용자가 조합한 자모 배열이 사전에 존재하는 단어인지 판단

4. 한글 자모 분해 로직
- 완성형 한글을 자모 단위로 완전 분해 (복자음/복모음 포함)
- 유니코드 ‘가’(0xAC00)부터 시작
- 초성/중성/종성 분리
- 복자음/복모음 분해는 complexXXXMap을 참조

